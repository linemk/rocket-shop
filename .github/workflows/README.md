# GitHub Actions Workflows

Этот каталог содержит GitHub Actions workflows для CI/CD проекта rocket-shop.

## 📋 Структура

### Основной workflow

- **`ci.yml`** - главный CI pipeline, который:
  - Извлекает переменные из Taskfile.yml
  - Запускает линтинг (lint)
  - Запускает юнит-тесты (test)
  - Запускает интеграционные тесты (integration-test)

### Reusable workflows

- **`lint-reusable.yml`** - линтинг кода через golangci-lint
- **`test-reusable.yml`** - юнит-тесты для всех модулей
- **`test-integration-reusable.yml`** - интеграционные тесты с testcontainers

## 🔄 Pipeline Flow

```
extract-vars
    ↓
   lint → test → integration-test
```

1. **extract-vars**: извлекает версии Go, golangci-lint и список модулей из Taskfile.yml
2. **lint**: запускает линтер параллельно после извлечения переменных
3. **test**: запускает юнит-тесты параллельно после извлечения переменных
4. **integration-test**: запускает интеграционные тесты после успешного прохождения unit-тестов

## 🧪 Integration Tests

Интеграционные тесты используют:
- **Docker** для запуска контейнеров с зависимостями (MongoDB, PostgreSQL)
- **testcontainers-go** для управления жизненным циклом контейнеров
- **Taskfile** для запуска тестов: `task test-integration`

### Особенности

- Каждый тест запускается в изолированном окружении
- Автоматическая очистка контейнеров после тестов
- Генерация .env файлов для всех сервисов

## 🚀 Локальный запуск

Все workflows можно запустить локально через Taskfile:

```bash
# Линтинг
task lint

# Юнит-тесты
task test

# Интеграционные тесты
task test-integration
```

## 📝 Добавление новых модулей

При добавлении нового модуля:

1. Добавьте имя модуля в переменную `MODULES` в `Taskfile.yml`
2. Workflows автоматически подхватят новый модуль
3. Создайте директорию `tests/integration` в модуле (опционально)

## 🔧 Настройка

Версии инструментов настраиваются в `Taskfile.yml`:

```yaml
vars:
  GO_VERSION: '1.24'
  GOLANGCI_LINT_VERSION: 'v2.1.5'
  MODULES: assembly inventory order payment platform iam notification
```

Скрипт `.github/scripts/extract-versions.sh` автоматически извлекает эти значения для workflows.

